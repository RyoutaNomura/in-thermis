# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET   /                                  controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET   /assets/*file                      controllers.Assets.versioned(path="/public", file: Asset)

# GET   /search                            controllers.Application.doSearch(text, order, fetchSize, currentPage)
GET   /search                            controllers.Application.doSearch
GET   /runIndexer                        controllers.Application.runIndexer

GET   /loadInitData                      controllers.Application.loadInitData
